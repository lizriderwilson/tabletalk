<div class="container mx-auto mt-4 flex">

    <div class="w-2/3">
        <div class="bg-primary-100 flex flex-col px-8 pb-4" id="description">
            <div class="py-8 flex justify-between items-center">
                <h2 class="header mb-2"><%= @character.name %></h2>
                <%= link_to "Edit Character", edit_campaign_character_path(@campaign, @character), class: "btn" %>
            </div>
            <div>
                <p><%= @character.bio %></p>
            </div>
        </div>
    </div>

    <div class="bg-primary-200 h-screen w-1/3">

        <div class="px-6 py-4" id="players">
            <h3 class="text-2xl font-semibold">Items</h3>
        </div>

    </div>
</div>