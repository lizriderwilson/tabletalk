<div class="container mx-auto">
    <div class="px-6 py-4" id="sessions">
        <h3 class="text-2xl font-semibold">Sessions</h3>
        <%= month_calendar(events: @seshions) do |date, seshions| %>
            <%= date.day %>
            <% seshions.each do |seshion| %>
                <div>
                    <%= seshion.start_time.to_formatted_s(:twelvehrs) %>
                </div>
            <% end %>
        <% end %>
    </div>
</div>